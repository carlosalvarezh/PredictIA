# --- Core numérico / datos ---
numpy>=1.26,<2.0
pandas>=2.2
pyarrow>=17.0.0        # IO rápido (parquet/csv)
openpyxl>=3.1          # lectura de Excel para GenAI (cesantías)

# --- Modelado clásico / ML ---
scikit-learn>=1.5
xgboost>=2.0           # opcional: puedes cambiar por lightgbm>=4.3
joblib>=1.4

# --- Series de tiempo ---
statsmodels>=0.14
pmdarima>=2.0          # auto-ARIMA (práctico para baselines)

# --- Métricas y confiabilidad ---
mapie>=0.8             # conformal prediction para regresión/clasificación

# --- Explicabilidad ---
shap>=0.45

# --- Visualización ---
matplotlib>=3.8

# --- API / servicio ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
orjson>=3.10           # respuestas FastAPI más rápidas

# --- Utilidades ---
python-dotenv>=1.0
loguru>=0.7
httpx>=0.27            # requests en pruebas

# --- Testing ---
pytest>=8.2, <9
pytest-cov>=5,<8

seaborn>=0.13,<0.14
